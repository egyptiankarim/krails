<!DOCTYPE html>
<html lang="en">
  <%= render partial: "shared/head" %>
  <body>

    <!-- Wrap all page content here -->
    <div id="wrap">
      <%= render partial: "shared/nav" %>

      <!-- Begin page content -->
      <div class="container">
        <%= yield :main %>
      </div>
    </div>
    <!-- Close up the page wrapping. -->

    <%= render partial: "shared/foot" %>

    <%= render partial: "shared/scripts" %>
  </body>
</html>
